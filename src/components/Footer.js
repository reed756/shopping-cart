const Footer = (props) => {
    return (
        <div className={props.class}>
            <footer>
                <h1>{props.footer}</h1>
            </footer>
        </div>
    )
}

export default Footer